# Envio do formulario de orcamento (/api/send-email)
# Configurar como Environment Variables no projeto da Vercel — nunca comitar o .env real.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
# Destinatario dos leads do formulario
CONTATO_DESTINO=
